services:
  php:
    user: ${UID:-1000}:${GID:-1000}
    build:
      context: ./
      dockerfile: Dockerfile
      args:
        PHP_VERSION: ${PHP_VERSION:-8.3}
    ports:
      - "8080:80"
    volumes:
      - ./:/app
      - /app/vendor
